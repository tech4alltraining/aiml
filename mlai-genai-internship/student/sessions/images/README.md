# Session plots

The charts embedded in the session guides.

**These are generated, not hand-made.** To reproduce them:

```bash
conda activate genai && python generate_plots.py
```

Each image is the output of code that appears in the guide, run against the
real dataset — so what a student sees on the page is what they will get when
they run the cell themselves.
